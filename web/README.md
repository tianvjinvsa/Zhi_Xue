# 智题坊 Web 版

基于 **Vue 3 + Element Plus + FastAPI** 构建的现代化智能答题系统。

## 技术栈

### 前端

- **Vue 3** - 渐进式 JavaScript 框架
- **Vue Router** - 官方路由管理器
- **Pinia** - 状态管理
- **Element Plus** - UI 组件库
- **Vite** - 构建工具
- **Axios** - HTTP 请求库
- **SCSS** - CSS 预处理器

### 后端

- **FastAPI** - 高性能 Python Web 框架
- **Uvicorn** - ASGI 服务器
- **Pydantic** - 数据验证

## 项目结构

```
web/
├── frontend/                # Vue 前端项目
│   ├── src/
│   │   ├── api/            # API 接口
│   │   ├── router/         # 路由配置
│   │   ├── styles/         # 全局样式
│   │   └── views/          # 页面组件
│   ├── package.json
│   └── vite.config.js
│
└── backend/                 # FastAPI 后端
    └── main.py             # API 服务入口
```

## 快速开始

### 1. 安装依赖

#### 后端依赖

```bash
pip install fastapi uvicorn
```

#### 前端依赖

```bash
cd web/frontend
npm install
```

### 2. 启动服务

#### 启动后端 API 服务

```bash
cd web/backend
python main.py
# 或使用 uvicorn
uvicorn main:app --reload --port 8000
```

#### 启动前端开发服务器

```bash
cd web/frontend
npm run dev
```

### 3. 访问系统

- 前端地址：<http://localhost:3000>
- API 文档：<http://localhost:8000/docs>

## 功能特性

### 📚 题库管理

- 创建、编辑、删除题库
- 添加单选题、多选题、判断题、填空题
- 支持题目难度设置
- 题目筛选和搜索

### 📝 试卷管理

- 根据配置自动生成试卷
- 支持设置题目数量、难度范围
- 自定义分值规则
- 试卷预览功能

### ✏️ 在线答题

- 倒计时功能
- 答题卡快速导航
- 自动保存答案
- 支持暂停和继续

### 📊 成绩分析

- 得分统计
- 正确率分析
- 错题查看
- 答案解析

### 🤖 AI 功能

- 文本智能解析题目
- AI 自动生成题目
- 图片 OCR 识别（规划中）

## API 接口

### 题库相关

- `GET /api/banks` - 获取所有题库
- `POST /api/banks` - 创建题库
- `GET /api/banks/{id}` - 获取题库详情
- `PUT /api/banks/{id}` - 更新题库
- `DELETE /api/banks/{id}` - 删除题库

### 题目相关

- `GET /api/banks/{bank_id}/questions` - 获取题库题目
- `POST /api/banks/{bank_id}/questions` - 添加题目
- `PUT /api/banks/{bank_id}/questions/{question_id}` - 更新题目
- `DELETE /api/banks/{bank_id}/questions/{question_id}` - 删除题目

### 试卷相关

- `GET /api/papers` - 获取所有试卷
- `POST /api/papers/generate` - 生成试卷
- `GET /api/papers/{id}` - 获取试卷详情
- `GET /api/papers/{id}/questions` - 获取试卷题目
- `DELETE /api/papers/{id}` - 删除试卷

### 考试相关

- `POST /api/exam/start/{paper_id}` - 开始考试
- `POST /api/exam/{exam_id}/answer` - 提交答案
- `POST /api/exam/{exam_id}/submit` - 交卷

### 成绩相关

- `GET /api/results` - 获取所有成绩
- `GET /api/results/{id}` - 获取成绩详情
- `DELETE /api/results/{id}` - 删除记录

### AI 相关

- `POST /api/ai/parse` - AI 解析题目
- `POST /api/ai/generate` - AI 生成题目
- `GET /api/ai/check` - 检查 AI 连接

## 开发说明

### 前端开发

```bash
cd web/frontend
npm run dev     # 开发模式
npm run build   # 构建生产版本
npm run preview # 预览生产版本
```

### 后端开发

```bash
cd web/backend
uvicorn main:app --reload  # 开发模式（自动重载）
```

## 与原桌面版对比

| 特性 | 桌面版 (PyQt) | Web版 (Vue) |
|------|--------------|-------------|
| 跨平台 | 需要打包 | 浏览器直接访问 |
| 界面美观度 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 开发效率 | 中等 | 高 |
| 部署方式 | 本地安装 | 服务器部署 |
| 多用户支持 | 单用户 | 支持多用户 |

## 许可证

MIT License
